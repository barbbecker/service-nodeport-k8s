apiVersion: v1
kind: Service
metadata:
  name: hello-barbara
  labels:
    run: hello-barbara
spec:
  type: NodePort
  selector:
    app: hello-barbara
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30010